class Ready {
    constructor() {
        this.name = 'ready'
    }
    async run() {
        console.log(`${this.Client.user.username} isway onlineway onway ${this.Client.guilds.cache.size} erverssay!`);
    }
}

module.exports = Ready